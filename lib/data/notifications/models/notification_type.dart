enum NotificationType {
  reminder,      // reminders for items, deadlines, schedules
  security,      // login alerts, password changes, suspicious sign-in
  general,       // general updates (app updates, feature announcements)
  shared,        // someone shared a list with you
  activity,      // collaborator updates: edits, changes, comments
}
